const numbers = [22, 3, 4, 5, 6, 7];

numbers.forEach((number) => {
    console.log(number);
});


const integers = [2, 3, 4, 5, 6, 7];
integers.forEach((integer, idx) => {
    console.log(` ${idx} : ${integer}`);
});

