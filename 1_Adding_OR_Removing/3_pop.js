const numbers = [33,23,45,667,111];

const last = numbers.pop(); 
// it returns the remove element and it change the original array also. if array is empty the it returns undefined
console.log(numbers);
console.log(last);

